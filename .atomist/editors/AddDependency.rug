@description "Add a dependency to elm-package.json"
@tag "elm"
editor AddDependency


param dependency_name: .*

param dependency_version: .*

let dependencies = $(/[name='elm-package.json']/->json/dependencies)

with dependencies
 do addKeyValue dependency_name dependency_version
